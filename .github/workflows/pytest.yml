name: Python Testing Workflow

on:
  push:
    branches:
      - main
      - dev

jobs:
  test-python:
    runs-on: self-hosted
    steps:
      - name: Update PATH
        run: |
          echo 'C:\Users\louisli\AppData\Local\Programs\Python\Python311\Scripts' >> $Env:PATH
        shell: cmd

      - name: Run Pytest
        run: pytest C:\Users\louisli\Desktop\githubAction\githubAction_test\test_script.py
        shell: cmd